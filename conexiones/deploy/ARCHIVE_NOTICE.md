Nota: los artefactos compilados y backups de workflows ya no se almacenan en `conexiones/deploy` para evitar confusiones con el código fuente de desarrollo.

He movido una copia de `wf-procesar-factura.js` y `wf-procesar-factura.metadata.json` a `archivos_archivo/wf-procesar-factura/` como archivo de referencia. Si prefieres que los elimine por completo, indícalo y procederé a borrarlos del repositorio.
